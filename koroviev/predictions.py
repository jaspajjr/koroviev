def make_predictions(model, fixture_array):
    """
    This function takes the coefficients from a fitted model, and an array of
     fixtures and returns the predicted results of those fixtures.
    """
    expected_results_list = []

    return expected_results_list

print "Hello World"
